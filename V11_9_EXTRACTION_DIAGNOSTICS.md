# V11.9 Extraction Diagnostics

- Reports the exact failed embedded-JDK verification item and observed file size.
- Uses the known JDK 17 modules image size (128,712,865 bytes) instead of reopening the asset for verification.
- Forces a clean extraction using foundation marker v11.9.
